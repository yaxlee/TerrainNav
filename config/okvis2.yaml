%YAML:1.0
cameras:
  - {
    T_SC:
        [  0.02113245, -0.00881263,  0.99973784,  0.02509606,
          -0.99974295, -0.00840052,  0.02105851,  0.0471044,
           0.00821274, -0.99992588, -0.00898789, -0.04708322,
           0.0,         0.0,         0.0,         1.0        ],  # T_ic
           
        image_dimension: [960, 540],

        focal_length: [457.0678775659748, 455.91330793145687],
        principal_point: [479.0466997316015, 271.48876457767676],
        distortion_coefficients: [-0.000691831828701086, 0.0005776084531271573, 0.0007899179317230896, 3.1645124960775234e-05],
        distortion_type: radialtangential,
        cam_model: pinhole,
        camera_type: gray, #gray, rgb, gray+depth, rgb+depth
        mapping: true,
        mapping_rectification: false,
        slam_use: okvis} #none, okvis, okvis-depth, okvis-virtual

# additional camera parameters
camera_parameters:
    timestamp_tolerance: 0.005 # [s] stereo frame out-of-sync tolerance
    sync_cameras: [0] # the cameras that will be synchronised
    image_delay: 0.07260778530900726
    online_calibration: # some parameters to set the online
        do_extrinsics: false # Do we online-calibrate extrinsics?
        do_extrinsics_final_ba: false # Do we calibrate extrinsics in final BA?
        sigma_r: 0.01 # T_SCi position prior stdev [m]
        sigma_alpha: 0.05 # T_SCi orientation prior stdev [rad]
        sigma_r_final_ba: 0.03 # T_SCi position prior stdev in final BA [m]
        sigma_alpha_final_ba: 0.3 # T_SCi orientation prior stdev in final BA [rad]
    # deep_stereo_indices: [0,1] # [left, right] camera indices for the stereo network
    fov_scale: 1.0 # Divisor for new focal length which is only used when fisheye stereo rectification for dense mapping.

imu_parameters:
    use: true
    a_max: 150.0 # acceleration saturation [m/s^2]
    g_max: 35.0 # gyro saturation [rad/s]
    sigma_g_c: 0.002 # gyro noise density [rad/s/sqrt(Hz)]
    sigma_a_c: 0.005 # accelerometer noise density [m/s^2/sqrt(Hz)]
    sigma_bg: 0.01 # gyro bias prior [rad/s]
    sigma_ba: 0.1 # accelerometer bias prior [m/s^2]
    sigma_gw_c: 0.000004 # gyro drift noise density [rad/s^s/sqrt(Hz)]
    sigma_aw_c: 0.00004
    g: 9.81007 # Earth's acceleration due to gravity [m/s^2]
    g0: [ 0.0, 0.0, 0.0 ] # initial gyro bias [rad/s]
    a0: [ 0.0, 0.0, 0.0 ] # initial accelerometer bias [m/s^2]
    s_a: [ 1.0,  1.0, 1.0 ] # scale factor for accelerometer measurements: a_true = s_a * a_meas + b_a
    # transform Body-Sensor (IMU)
    T_BS:
        [1.0000, 0.0000, 0.0000, 0.0000,
         0.0000, 1.0000, 0.0000, 0.0000,
         0.0000, 0.0000, 1.0000, 0.0000,
         0.0000, 0.0000, 0.0000, 1.0000]

frontend_parameters:
    detection_threshold: 42.0 # detection threshold. By default the uniformity radius in pixels
    absolute_threshold: 150.0 # absolute Harris corner threshold (noise floor)
    matching_threshold: 45.0 # BRISK descriptor matching threshold
    octaves: 0 # number of octaves for detection. 0 means single-scale at highest resolution
    max_num_keypoints: 1000 # restrict to a maximum of this many keypoints per image (strongest ones)
    keyframe_overlap: 0.50 # minimum field-of-view overlap
    use_cnn: false # use the CNN (if available) to filter out dynamic content / sky
    parallelise_detection: false # Run parallel detect & describe.
    num_matching_threads: 4 # Parallelise matching with this number of threads.
    # Rectangular regions excluded from feature detection: flat list of [x, y, w, h] groups.
    # Multiple rects: [x0, y0, w0, h0, x1, y1, w1, h1, ...]. Remove to disable.
    mask_rects: [0, 400, 960, 140]

stationary_parameters:
    enabled: true
    entry_frames: 5
    exit_frames: 3
    min_landmarks: 20
    max_median_pixel_displacement: 0.45
    max_mean_pixel_displacement: 0.90
    sigma_v: 0.02
    sigma_position: 0.03
    sigma_orientation: 0.01

estimator_parameters: 
    num_keyframes: 11 # number of keyframes in optimisation window
    num_loop_closure_frames: 5 # number of loop closure frames in optimisation window
    num_imu_frames: 11 # number of frames linked by most recent nonlinear IMU error terms
    do_loop_closures: true # whether to do VI-SLAM or VIO
    do_final_ba: false # Whether to run a full final BA
    enforce_realtime: false # whether to limit the time budget for optimisation
    realtime_min_iterations: 2 # minimum number of iterations always performed
    realtime_max_iterations: 20 # never do more than these, even if not converged
    realtime_time_limit: 0.025 # time budget for realtime optimisation [s]
    realtime_num_threads: 4 # number of threads for the realtime optimisation
    full_graph_iterations: 5 # don't do more than these for the full (background) optimisation
    full_graph_num_threads: 4 # number of threads for the full (background) optimisation
    p_dbow: 0.4 # Match threshold for dBoW -- unfortunately this varies with setups.
    drift_percentage_heuristic: 20.0 # % allowed drift in loop closures rel. to dist. travelled.

output_parameters:
    display_topview: true # Displays top view of the trajectory. Might become slow for long sequences.
    display_matches: true  # displays debug video and matches. May be slow.
    display_overhead: false # debug overhead image. Is slow.
    enable_submapping: false # Enable volumetric submapping

gps_parameters:
  data_type: geodetic
  r_SA: [0.0, 0.0, 0.0]
  yaw_error_threshold: 5.0
  robust_gps_init: false
  gps_sigma_scale: 3.0
  gps_outlier_scale: 3.0  # Multiplier on 3-sigma rejection threshold (1.0=6m, 3.0=18m for sigma=2m)
  gps_dropout_threshold: 10.0  # [s] Min VIO time since last GPS state before re-init triggers (prevents false re-init after GPS loop closure)
  gps_max_correction: 100.0  # [m] Max T_GW translation correction allowed; larger corrections are rejected as bad Umeyama estimates
  gps_min_init_points:3  # Min GPS points for Umeyama alignment (>=3 always enforced; higher = more robust against sharp-turn corrupted estimates)
  max_h_err: 20.0      # [m] Reject GPS if horizontal error exceeds this
  max_v_err: 80.0      # [m] Reject GPS if vertical error exceeds this
  min_fix_status: 1    # Reject GPS if fix_status < this (0=no filter, 1=reject status 0)
  geoid_model: "egm96-5"  # GeographicLib geoid model for ellipsoidal->orthometric height correction

# dem_parameters:
#   use: true
#   sigma_h: 1
#   d_above_ground: 2
#   r_SA: [0, 0, 0]
#   use_dem_height_for_gps: false
#   dem_fusion_alpha: 0.5   # GPS weight in altitude fusion: 0=full DEM, 1=full GPS
